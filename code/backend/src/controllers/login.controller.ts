import { LoginResquest } from "../models/login.model";

function loginManager(loginReq: LoginResquest): boolean {
  return true;
}

function loginPlayer(loginReq: LoginResquest): boolean {
  return true;
}

export { loginManager, loginPlayer };
